# Frontend - AI RAG Assistant

## Run locally

```bash
cd frontend
npm install
npm start
```

Open http://localhost:4200.

Update `src/app/chat.service.ts` if the backend URL changes after deployment.
